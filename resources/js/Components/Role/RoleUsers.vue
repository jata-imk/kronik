<script setup>
const props = defineProps({
    role: Object,
});

// fetch users
// const { data: users } = await useFetch(`/api/admin/roles/${props.role.id}/users`);
</script>

<template>
    <div>
        Aquí irían los usuarios
    </div>
</template>